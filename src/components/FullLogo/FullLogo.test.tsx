import { FullLogo } from './FullLogo';
import { render } from '@testing-library/react';

describe('FullLogo Component', () => {
  it('should render Full Logo', () => {
    render(<FullLogo />);
  });
});
