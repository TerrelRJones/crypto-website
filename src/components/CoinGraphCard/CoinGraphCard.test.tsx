import { render } from '@testing-library/react';
import CoinGraphCard from 'components/CoinGraphCard';

describe('CoinGraphCard', () => {
  it('should render CoinGraphCard', () => {
    render(<CoinGraphCard />);
  });
});
